{{- if .Values.rbacHelpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
    labels:
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        app.kubernetes.io/name: {{ include "dojo-operator.name" . }}
        helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    name: {{ include "dojo-operator.resourceName" (dict "suffix" "dojo-editor-role" "context" $) }}
rules:
    - apiGroups:
        - core.jutsu.com
      resources:
        - dojoes
      verbs:
        - create
        - delete
        - get
        - list
        - patch
        - update
        - watch
    - apiGroups:
        - core.jutsu.com
      resources:
        - dojoes/status
      verbs:
        - get
{{- end }}
